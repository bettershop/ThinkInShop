<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.laiketui.common.mapper.RoleMenuModelMapper" >
  <resultMap id="BaseResultMap" type="com.laiketui.domain.role.RoleMenuModel" >
    <!--
      WARNING - @mbg.generated
    -->
    <result column="role_id" property="role_id" jdbcType="INTEGER" />
    <result column="menu_id" property="menu_id" jdbcType="INTEGER" />
    <result column="add_date" property="add_date" jdbcType="TIMESTAMP" />
  </resultMap>

  <!-- 新增：根据角色 ID 批量删除所有权限菜单 -->
  <delete id="deleteByRoleId" parameterType="java.lang.Integer">
    DELETE FROM lkt_role_menu
    WHERE role_id = #{roleId}
  </delete>

  <!-- 新增：批量插入权限菜单（使用 INSERT IGNORE 避免重复插入） -->
  <insert id="batchInsertIgnore" parameterType="java.util.List">
    INSERT IGNORE INTO lkt_role_menu (role_id, menu_id, add_date)
    VALUES
    <foreach collection="roleMenuList" item="item" separator=",">
      (
      #{item.role_id, jdbcType=INTEGER},
      #{item.menu_id, jdbcType=INTEGER},
      #{item.add_date, jdbcType=TIMESTAMP}
      )
    </foreach>
  </insert>

  <delete id="deleteByRoleAndMenuIds">
    DELETE FROM lkt_role_menu
    WHERE role_id = #{roleId}
    AND menu_id IN
    <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
      #{menuId}
    </foreach>
  </delete>


</mapper>