<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.laiketui.common.mapper.GuideMenuModelMapper" >
  <resultMap id="BaseResultMap" type="com.laiketui.domain.role.GuideMenuModel" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="store_id" property="store_id" jdbcType="INTEGER" />
    <result column="role_id" property="role_id" jdbcType="INTEGER" />
    <result column="menu_id" property="menu_id" jdbcType="INTEGER" />
    <result column="is_display" property="is_display" jdbcType="TINYINT" />
    <result column="guide_sort" property="guide_sort" jdbcType="INTEGER" />
    <result column="add_date" property="add_date" jdbcType="TIMESTAMP" />
  </resultMap>
  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbg.generated
    -->
    id, store_id, role_id, menu_id, is_display, guide_sort, add_date
  </sql>

  <!-- 新增：根据角色 ID 批量删除所有功能导览菜单 -->
  <delete id="deleteByRoleId" parameterType="java.lang.Integer">
    DELETE FROM lkt_guide_menu
    WHERE role_id = #{roleId}
  </delete>

  <!-- 新增：批量插入功能导览菜单 -->
  <insert id="batchInsert" parameterType="java.util.List">
    INSERT INTO lkt_guide_menu (store_id, role_id, menu_id, guide_sort, add_date)
    VALUES
    <foreach collection="guideMenuList" item="item" separator=",">
      (
      #{item.store_id, jdbcType=INTEGER},
      #{item.role_id, jdbcType=INTEGER},
      #{item.menu_id, jdbcType=INTEGER},
      #{item.guide_sort, jdbcType=INTEGER},
      #{item.add_date, jdbcType=TIMESTAMP}
      )
    </foreach>
  </insert>

  <!-- 批量删除导览菜单：根据角色ID + 菜单ID集合 -->
  <delete id="deleteGuideByRoleAndMenuIds">
    DELETE FROM lkt_guide_menu
    WHERE role_id = #{roleId}
    AND menu_id IN
    <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
      #{menuId}
    </foreach>
  </delete>

</mapper>