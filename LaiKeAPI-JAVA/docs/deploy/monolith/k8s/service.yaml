apiVersion: v1
kind: Service
metadata:
  name: laike-apis
  namespace: laike-prod
spec:
  selector:
    app: laike-apis
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 21898
  type: ClusterIP
