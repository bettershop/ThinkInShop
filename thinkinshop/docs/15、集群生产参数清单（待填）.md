# 集群生产参数清单（待填）

## 1. 说明

本文用于上线前收集生产环境参数，填写后可直接替换到：

- `docs/deploy/cluster/bootstrap.cluster.prod.example.yml`
- `docs/deploy/cluster/k8s/secret.example.yaml`

## 2. 基础信息

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| 环境标识 | `prod` |  |
| 对外域名 | `api.example.com` |  |
| 上线日期 | `2026-03-23` |  |
| 负责人 | `ops@company.com` |  |

## 3. MySQL RDS

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| RDS 地址 | `rm-xxxx.mysql.rds.aliyuncs.com` |  |
| 端口 | `3306` |  |
| 数据库名 | `v3_db` |  |
| 用户名 | `laike_app` |  |
| 密码 | `******` |  |
| JDBC 完整串 | `jdbc:mysql://...` |  |

## 4. Redis Cluster

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| 节点列表 | `r1:6379,r2:6379,r3:6379` |  |
| 密码 | `******` |  |
| 超时(ms) | `30000` |  |
| 连接池 max-active | `50` |  |
| 连接池 max-idle | `20` |  |
| 连接池 min-idle | `5` |  |

## 5. OSS

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| Endpoint | `oss-cn-hangzhou.aliyuncs.com` |  |
| Bucket | `laike-prod` |  |
| AccessKeyId | `LTAI...` |  |
| AccessKeySecret | `******` |  |
| 访问域名（可选） | `https://cdn.example.com` |  |

## 6. SLS

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| SLS Project | `laike-prod-log` |  |
| Logstore | `laike-apis` |  |
| 采集方式 | `ECS-Logtail` / `K8s-stdout` |  |
| 告警通知人 | `手机号/钉钉群` |  |

## 7. WebSocket / SLB

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| SLB 空闲超时(秒) | `3600` |  |
| Ingress read timeout(秒) | `3600` |  |
| 是否启用 HTTPS/WSS | `是` |  |
| 是否保留会话粘性 | `否（已支持跨节点）` |  |

## 8. JVM 与容量

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| 副本数（初始） | `3` |  |
| CPU request/limit | `1000m / 4` |  |
| 内存 request/limit | `2Gi / 4Gi` |  |
| JVM 参数 | `-Xms2g -Xmx2g -XX:+UseG1GC` |  |

## 9. 发布版本

| 参数项 | 示例/格式 | 你的值 |
|---|---|---|
| 镜像仓库 | `registry.example.com/laike` |  |
| 镜像标签 | `v20260323-01` |  |
| 回滚标签 | `v20260320-03` |  |

## 10. 结果确认

上线前至少确认：

1. RDS、Redis、OSS、SLS 参数已全部填充。
2. 所有密码不在明文文档流转，已进入 Secret/密钥系统。
3. 回滚版本号已明确且可用。
