<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.laiketui.common.mapper.BackUpRecordModelMapper">
    <select id="recordList" resultType="map" parameterType="map">
        select * from lkt_backup_record
        where store_id = #{store_id} and recycle=0
        <if test="pageStart != null and pageEnd != ''">limit #{pageStart},#{pageEnd}</if>
    </select>

    <select id="countRecord" parameterType="map" resultType="int">
        select count(1) from lkt_backup_record
        where store_id = #{store_id} and recycle=0;
    </select>
</mapper>
